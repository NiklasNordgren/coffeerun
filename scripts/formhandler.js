(function(window) {
  'use strict';
  var App = window.App || {};
  var $ = window.jQuery;

  function FormHandler() {
    // Code will go here
  }

  App.FormHandler = FormHandler;
  window.App = App;
})(window);
